= bootstrap_form_for @user, url: signup_path, html: { class: 'form-signin' } do |form|
  = form.text_field :display_name, value: ''
  = form.email_field :email
  = form.password_field :password
  = form.password_field :password_confirmation, label: 'Password again'
  = form.submit 'Sign up', class: 'btn btn-lg btn-primary btn-block'